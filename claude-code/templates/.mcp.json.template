{
  "_comment": "See README.md and claude-code/QUICKSTART.md for setup instructions. This file is auto-generated from templates/.mcp.json.template",
  "mcpServers": {
    "serena": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${SERENA_PATH}",
        "serena",
        "start-mcp-server",
        "--context",
        "claude-code",
        "--project",
        "${PROJECT_ROOT}"
      ],
      "env": {
        "PATH": "${HOME}/.claude/scripts/gopls-shim:${PATH}"
      },
      "alwaysLoad": true,
      "_comment": "env.PATH: serena が起動する gopls だけを scripts/gopls-shim 経由 (-remote=auto、daemon 共有) にする。Serena MCP v1.3.0+ — start-mcp-server サブコマンド + --context claude-code（旧 serena-mcp-server は廃止）。alwaysLoad: tool-search deferral skip (頻繁利用、2.1.121+)"
    }
  }
}
